# README hero asset

Drop the hero image/GIF here as `hero.gif` (or change the extension in the main
`README.md` `<img src=...>` if you'd rather ship a static `hero.png`).

**What to capture:** ~10–15 seconds of the actual 10-minute loop the README
already describes — Generate a prompt → **Send to ComfyUI** → the result landing
in **Gallery**. That's the single clearest demonstration of what this app does
that a bullet list can't convey. A still screenshot of the Generate page with a
finished result showing works fine too if a GIF is more effort than it's worth
right now.

Keep it under ~5MB so the README doesn't feel slow to load on GitHub. Once it's
here, remove the TODO HTML comment right below the `<img>` tag in `README.md`.
