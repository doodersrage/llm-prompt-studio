# README hero asset

Optional. Drop a hero image/GIF here as `hero.gif` (or `hero.png`) and add an
`<img>` under the badges in the main `README.md` if you want a visual.

**What to capture:** ~10–15 seconds of the flagship Play loop — Campaign →
Moodboard → Fitting → Day → **Cut film** → Save to Cast — or the shorter still →
Video I2V → Gallery path. Keep it under ~5MB.

The README no longer references a missing file; shipping without a hero is fine.
